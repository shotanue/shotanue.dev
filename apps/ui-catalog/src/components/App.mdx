import { Canvas, Meta } from '@storybook/blocks';

import * as AppStories from './App.stories'

<Meta of={AppStories} />

# App

App is a sample component that is used to demonstrate the usage of Storybook.

<Canvas of={AppStories.Default} />